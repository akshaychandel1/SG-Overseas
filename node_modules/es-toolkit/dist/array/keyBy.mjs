function keyBy(arr, getKeyFromItem) {
    const result = {};
    for (const item of arr) {
        const key = getKeyFromItem(item);
        result[key] = item;
    }
    return result;
}

export { keyBy };
