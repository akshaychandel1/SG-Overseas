function uniqBy(arr, mapper) {
    const map = new Map();
    for (const item of arr) {
        const key = mapper(item);
        if (!map.has(key)) {
            map.set(key, item);
        }
    }
    return Array.from(map.values());
}

export { uniqBy };
