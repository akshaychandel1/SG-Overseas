function omitBy(obj, shouldOmit) {
    const result = {};
    for (const [key, value] of Object.entries(obj)) {
        if (shouldOmit(value, key)) {
            continue;
        }
        result[key] = value;
    }
    return result;
}

export { omitBy };
