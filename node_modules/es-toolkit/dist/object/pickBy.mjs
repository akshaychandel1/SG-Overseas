function pickBy(obj, shouldPick) {
    const result = {};
    for (const [key, value] of Object.entries(obj)) {
        if (!shouldPick(value, key)) {
            continue;
        }
        result[key] = value;
    }
    return result;
}

export { pickBy };
