const IS_PLAIN = /^\w*$/;
const IS_DEEP = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
function isDeepKey(key) {
    switch (typeof key) {
        case 'number':
        case 'symbol': {
            return false;
        }
        case 'string': {
            return !IS_PLAIN.test(key) && IS_DEEP.test(key);
        }
    }
}

export { isDeepKey };
